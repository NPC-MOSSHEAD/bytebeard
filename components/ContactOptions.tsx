"use client";
import { useState } from "react";
import { site } from "@/content/site";
export default function ContactOptions() { const [copied, setCopied] = useState(false); return <div className="contact-options"><a href={site.github} target="_blank" rel="noopener noreferrer"><span>01 / SEE MY CODE</span><strong>GITHUB</strong><span className="option-arr">↗</span></a>{site.email && <a href={`mailto:${site.email}?subject=Hey%20Bytebeard`}><span>02 / SAY HELLO</span><strong>EMAIL</strong><span className="option-arr">↗</span></a>}{site.instagram && <a href={site.instagram} target="_blank" rel="noopener noreferrer"><span>03 / FIND ME</span><strong>INSTAGRAM</strong><span className="option-arr">↗</span></a>}<button type="button" onClick={async () => {try {await navigator.clipboard.writeText(site.github);setCopied(true);} catch {setCopied(false);}}}><span>02 / TAKE IT WITH YOU</span><strong>{copied ? "COPIED!" : "COPY MY GITHUB"}</strong><span className="option-arr">{copied ? "✓" : "↗"}</span></button></div>; }
